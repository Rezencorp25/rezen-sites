// ⚠️ PROTOTIPO — rules aperte. NON DEPLOYARE IN PRODUZIONE.
// Le rules production saranno riscritte in DOC 3 con auth + role checks.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
